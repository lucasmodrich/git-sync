# Shell scripts are executed inside Linux containers and must keep LF line
# endings regardless of the checkout platform's core.autocrlf setting —
# a CRLF shebang line breaks exec() with "No such file or directory" since
# the interpreter path becomes "/bin/sh\r".
*.sh text eol=lf
